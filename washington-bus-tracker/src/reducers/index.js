

const initialState = {
    busPosition: [],
    isLoading: false,
    error: ""
}

export const metroReducer = (state = initialState, action) => {
    switch(action.type) {
        default:
            return state;
    }
}